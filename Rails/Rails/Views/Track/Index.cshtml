@using Rails.Models
@model IEnumerable<Rails.Models.View.TrackSectorViewModel>

@{
    ViewBag.Title = "Index";
    string column = "col-xs-2 col-sm-2";
    string scolumn = "col-xs-2 col-sm-2";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<div class="container-fluid">
    <div class="row track-row track-row-heading">
        <div class="col-xs-1">
            @Html.DisplayNameFor(model => model.Track.Number)
        </div>
        <div class="@column">
            @Html.DisplayNameFor(model => model.Track.Depot)
        </div>
        <div class="col-xs-1">
            @Html.DisplayNameFor(model => model.Track.Length)
        </div>
        <div class="col-xs-1">
            @Html.DisplayNameFor(model => model.Track.Accessible)
        </div>
        <div class="col-xs-1">
            @Html.DisplayNameFor(model => model.Track.InOutTrack)
        </div>
    </div>
    <hr/>
    <div id="place-tram-container"></div>
    @foreach (var item in Model)
    {
        <div class="row track-row">
            <div class="col-xs-1">
                @Html.DisplayFor(modelItem => item.Track.Number)
            </div>
            <div class="@column">
                @Html.ActionLink(item.Track.Depot.Name, "Details", "Depot", new
           {
               id = item.Track.Depot.Id
           }, null)
            </div>
            <div class="col-xs-1">
                @Html.DisplayFor(modelItem => item.Track.Length)
            </div>
            <div class="col-xs-1">
                @Html.DisplayFor(modelItem => item.Track.Accessible)
            </div>
            <div class="col-xs-1">
                @Html.DisplayFor(modelItem => item.Track.InOutTrack)
            </div>
            <div class="col-xs-2 col-sm-2">
                @Html.ActionLink("Edit", "Edit", new { id = item.Track.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Track.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Track.Id })
            </div>
        </div>
        
        @Html.Partial("~/Views/Track/_Sectors.cshtml", item.Sectors)
        
    }
</div>